reference: "$COMPONENTS/classification/gnn-embedding/model.yaml"
ml_model: "GNN_EMBEDDING"
encoding_features: "GRAPH"
hyperparameters:
  embedding_size : [300]
# The original paper only used 256
#  embedding_size : [128, 256, 512]
